function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  // background("#111118");

  // circle(100, 50, 50)
  // fill("orange");
  // noStroke()
}

function mouseClicked() {
  // Creates new instance of Flower
  new Flower(mouseX, mouseY)
}
